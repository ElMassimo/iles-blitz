{
  "name": "iles-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "iles dev --open --host 0.0.0.0",
    "build": "iles build",
    "preview": "vite preview --open --port 5050",
    "typecheck": "vue-tsc --noEmit"
  },
  "devDependencies": {
    "iles": "^0.4.4-0",
    "typescript": "~4.3.5",
    "vite": "^2.6.10",
    "vue-tsc": "^0.3.0"
  }
}
